@model IEnumerable<PatientManagementProject.Models.tblSector>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SecName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblDay.DayName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SecName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblDay.DayName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SecId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SecId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SecId })
        </td>
    </tr>
}

</table>
